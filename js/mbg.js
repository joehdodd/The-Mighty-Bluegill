/*var parallaxHeight = $('.jumbotron').outerHeight();
function parallax(){
    var scrolled = $(window).scrollTop();
    $('.top-bg').css('height', (parallaxHeight-scrolled) + 'px');
}

$(window).scroll(function(e){
    parallax();
});*/